This is a sub theme of Zen.
It uses bundler, compass, and grunt.

To install grunt dependencies, from this theme folder, run:
npm install

To install gems, run:
bundle install

To watch for changes, run:
compass compile && grunt watch
